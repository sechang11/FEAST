# Build outputs -- produced by build/build-feast.sh, never committed.
build/obj/
build/test_feast
build/test_feast.exe
4.0/lib/*/
*.o
*.mod
*.a
*.so
*.dylib
*.dll
*.exe

# Python
# Virtualenvs under any of the usual names -- `.venv/` alone missed `gui/venv/`,
# which is how a 709 MB PySide6 tree got committed once.
.venv/
venv/
env/
**/venv/
**/.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/dist/

# GUI verification output
gui/gui_verify.png
gui/*.png

# OS noise
.DS_Store
Thumbs.db

# Generated site output and screenshots
web/static/*.html
web/shots/
web/_upload*
gui/qa_win*/
gui/qa*.png

# App bundles
dist/
dist-new/
build/pyi/
*.spec.bak
feast-selftest.txt
selftest.txt

# Licence signing key -- must never be committed or shipped
licensing/private_key.txt
licensing/issued.csv

# SPIKE is fetched on demand by build/build-spike.sh, not vendored:
# it is a separate package with its own licence.
build/spike/

# Packaged app bundles -- build artefacts, rebuilt by packaging/build-app.sh
release/
dist/
